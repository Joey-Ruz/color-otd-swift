import SwiftUI

/// Today — Feature module namespace.
///
/// Per the architecture: types are `Today.Scene`, `Today.Presenter`,
/// `Today.UseCase`, `Today.Router`, `Today.Assembly`, etc.
/// Each layer is added in a Today+*.swift extension.
enum Today {}
