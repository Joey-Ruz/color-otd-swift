import SwiftUI

/// ColorDetail — module shown when any color is opened from Archive / Search / Profile.
/// Reuses Today's hero+recipe+lens-index pattern, parameterized by a ColorEntity.
enum ColorDetail {}
